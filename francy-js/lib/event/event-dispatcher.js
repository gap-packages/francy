define(['id-helper'], function (idHelper) {
  "use strict";

  return {
    Client: {
      build: function build() {

      },
      fire: function fire() {
        //TODO this might have to interact with a websocket, so we should some how handle it in the main entry point as an option
      }
    },
    Server: {
      build: function build() {

      },
      fire: function fire() {
        //TODO this might have to interact with a websocket, so we should some how handle it in the main entry point as an option
      }
    }
  }

});
